package step3;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import step3.dto.ParticipationForm;

import static org.assertj.core.api.Assertions.assertThat;

class ControlTowerTest {

//    private ControlTower controlTower = new ControlTower();
//
//    private static final ParticipationForm MOCK_PARTICIPATION_FORM = new ParticipationForm(3,3);
//
//    @BeforeEach
//    void setUp() {
////        controlTower = new ControlTower(MOCK_PARTICIPATION_FORM);
//    }
//
//    @DisplayName("레이싱 결과 리스트의 크기가 ParticipationForm의 attemptCount와 같은지 확인")
//    @Test
//    void startCarRacing() {
//        assertThat(controlTower.startCarRacing(MOCK_PARTICIPATION_FORM).attemptResultsSize()).isEqualTo(MOCK_PARTICIPATION_FORM.getAttemptCount());
//    }

}