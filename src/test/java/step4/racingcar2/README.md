# 🚀 3단계 - 자동차 경주 (TEST)

* `RacingCar` 는 `RacingCarEngine` 을 통해 GO/STOP 을 조절하며 테스트를 진행한다.
* `RacingCarView` 는 `RacingCar` 의 상태를 보여준다.
* `RacingCarGameRoundView` 는 진행한 `RacingCarGameRound` 의 결과를 보여준다.
* `System.setOut` 을 통해 `System.out` 을 테스트한다.
