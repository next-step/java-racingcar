package car.step2;

import java.util.List;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class StringAddCalculator {

    public List<Integer> splitString(String text) {
        Matcher m = Pattern.compile("//(.)\n(.*)").matcher(text);
        return null;
    }

}
