package step4.racingCar;

import java.util.ArrayList;
import java.util.Map;

public class Rule {


    public static ArrayList<String> whoIsWinner(Map<String, Integer> carList) {

//        Map<String, Integer> sorted = carStatus.entrySet()
//                .stream()
//                .sorted(Map.Entry.<String,Integer>comparingByValue().reversed())
//                .collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue, (o1, o2)->o1, LinkedHashMap::new));

//
        ArrayList<String> winner = new ArrayList<>();
////        int maxNum = carList.firstEntry().getValue();
//        carList.forEach((name, score) -> {
//            if(maxNum == score){
//                winner.add(name);
//            }
//        });

        return winner;
    }
}
